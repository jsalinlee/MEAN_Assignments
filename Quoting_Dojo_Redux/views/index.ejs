<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Quoting Dojo</title>
        <link rel="stylesheet" type="text/css" href="/style.css">
    </head>
    <body>
        <h1>Welcome to Quoting Dojo</h1>
        <% if( typeof(errors) != "undefined" ) { %>
            <% for (var i in errors) { %>
                <h3><%= errors[i].message %></h3>
            <% } %>
        <% } %>
        <form action="/quotes" method="post">
            <label for="name">Your Name: <input type="text" name="name"></label>
            <label for="quote">Your Quote: <textarea name="quote" rows="8" cols="80"></textarea></label>
            <input type="submit" name="submit" value="Add my quote!">
        </form>
        <form action="/quotes">
            <input type="submit" name="skip" value="Skip to quotes!">
        </form>
    </body>
</html>
